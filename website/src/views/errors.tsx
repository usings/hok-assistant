import type { Component } from 'solid-js'

export const NotFound: Component = () => {
  return (
    <section class="flex-grow flex justify-center items-center">
      <IconWindsock width={200} height={200} />
    </section>
  )
}

export default NotFound
